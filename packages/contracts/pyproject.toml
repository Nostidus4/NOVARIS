[project]
name = "qshield-contracts"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
    "numpy>=1.26,<2.3",
    "pandas>=2.2,<3",
    "pandera>=0.20",
    "pyyaml>=6.0",
]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"
