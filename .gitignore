# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.DS_Store


# Local notebook / matplotlib caches
.mplconfig/
.ipython/

# Secrets — không bao giờ commit
.env
.env.local
*.env
!.env.example
!**/.env.example

# FiinPro là dữ liệu thương mại — chỉ dùng nội bộ, không commit
# (docs/data/2026-09-17-chuyen-nguon-gia-fiinpro.md §2 mục 14)
data/raw/Full_Prices.xlsx
data/raw/prices/*_fiinpro_*.csv
# Sinh từ FiinPro (thương mại) — repo public, không commit
data/processed/*.parquet

# Chi tiết từng instance thí nghiệm hybrid (~5MB/instance, ~160-190MB/thí nghiệm) — giữ local.
# Commit phần tổng hợp: manifest, report, paired_uplift, claim_checklist, transfer_parameters.
artifacts/runs/*/outputs/hybrid/*/instances/
